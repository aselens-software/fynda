"use strict";
var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var __param = (this && this.__param) || function (paramIndex, decorator) {
    return function (target, key) { decorator(target, key, paramIndex); }
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.ConnectionService = void 0;
const tsyringe_1 = require("tsyringe");
const ConnectionRequest_1 = require("../../domain/entities/ConnectionRequest");
const ConnectionStatus_1 = require("../../domain/enums/ConnectionStatus");
let ConnectionService = class ConnectionService {
    connectionRepository;
    constructor(connectionRepository) {
        this.connectionRepository = connectionRepository;
    }
    async sendRequest(senderId, receiverId, senderGuildId) {
        const existing = await this.connectionRepository.findByUsers(senderId, receiverId);
        if (existing) {
            throw new Error('A connection or request already exists between these users.');
        }
        const request = new ConnectionRequest_1.ConnectionRequest('', // Generated by DB
        senderId, receiverId, ConnectionStatus_1.ConnectionStatus.PENDING, new Date(), new Date(), senderGuildId);
        return this.connectionRepository.save(request);
    }
    async skipUser(senderId, receiverId) {
        const existing = await this.connectionRepository.findByUsers(senderId, receiverId);
        if (existing)
            return existing;
        const request = new ConnectionRequest_1.ConnectionRequest('', senderId, receiverId, ConnectionStatus_1.ConnectionStatus.SKIPPED, // Store as skipped so they don't match again
        new Date(), new Date());
        return this.connectionRepository.save(request);
    }
    async getPendingRequests(discordId) {
        return this.connectionRepository.findPendingRequests(discordId);
    }
    async getRequestById(requestId) {
        return this.connectionRepository.findById(requestId);
    }
    async getRequestByChannelId(channelId) {
        return this.connectionRepository.findByChannelId(channelId);
    }
    async respondToRequest(requestId, accept, senderChannelId, receiverChannelId) {
        const request = await this.connectionRepository.findById(requestId);
        if (!request) {
            throw new Error('Connection request not found.');
        }
        if (request.status !== ConnectionStatus_1.ConnectionStatus.PENDING) {
            throw new Error('This request has already been processed.');
        }
        if (accept) {
            if (!senderChannelId || !receiverChannelId)
                throw new Error('Channel IDs are required when accepting a request.');
            request.accept(senderChannelId, receiverChannelId);
        }
        else {
            request.reject();
        }
        return this.connectionRepository.update(request);
    }
    async handleReveal(channelId, discordId) {
        const request = await this.connectionRepository.findByChannelId(channelId);
        if (!request) {
            throw new Error('This channel is not associated with an active connection.');
        }
        if (request.status === ConnectionStatus_1.ConnectionStatus.REVEALED) {
            throw new Error('Identities are already revealed.');
        }
        const bothRevealed = request.reveal(discordId);
        const updated = await this.connectionRepository.update(request);
        return { bothRevealed, request: updated };
    }
    async endChat(channelId) {
        const request = await this.connectionRepository.findByChannelId(channelId);
        if (!request) {
            throw new Error('This channel is not associated with an active connection.');
        }
        request.end();
        return this.connectionRepository.update(request);
    }
};
exports.ConnectionService = ConnectionService;
exports.ConnectionService = ConnectionService = __decorate([
    (0, tsyringe_1.injectable)(),
    __param(0, (0, tsyringe_1.inject)('IConnectionRepository')),
    __metadata("design:paramtypes", [Object])
], ConnectionService);
